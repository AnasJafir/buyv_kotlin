import 'package:flutter/material.dart';

class AdminCjImportScreen extends StatelessWidget {
  const AdminCjImportScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('AdminCjImportScreen')),
      body: const Center(child: Text('Coming Soon — Work in Progress')),
    );
  }
}

