import 'package:flutter/material.dart';

class AdminAffiliateSalesScreen extends StatelessWidget {
  const AdminAffiliateSalesScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('AdminAffiliateSalesScreen')),
      body: const Center(child: Text('Coming Soon — Work in Progress')),
    );
  }
}

