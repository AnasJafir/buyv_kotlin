import 'package:flutter/material.dart';

class AdminPromoterWalletsScreen extends StatelessWidget {
  const AdminPromoterWalletsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('AdminPromoterWalletsScreen')),
      body: const Center(child: Text('Coming Soon — Work in Progress')),
    );
  }
}

