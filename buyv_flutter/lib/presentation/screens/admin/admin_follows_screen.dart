import 'package:flutter/material.dart';

class AdminFollowsScreen extends StatelessWidget {
  const AdminFollowsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('AdminFollowsScreen')),
      body: const Center(child: Text('Coming Soon — Work in Progress')),
    );
  }
}

