import 'package:flutter/material.dart';

class AdminProductManagementScreen extends StatelessWidget {
  const AdminProductManagementScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('AdminProductManagementScreen')),
      body: const Center(child: Text('Coming Soon — Work in Progress')),
    );
  }
}

