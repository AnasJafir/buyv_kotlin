import 'package:flutter/material.dart';

class AffiliateSalesScreen extends StatelessWidget {
  const AffiliateSalesScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('AffiliateSalesScreen')),
      body: const Center(child: Text('Coming Soon — Work in Progress')),
    );
  }
}

